# API Changelog 32.1.0 vs. 32.2.0


## API Changes

### GET /assessments_reports
-  endpoint added


### POST /assessments_reports
-  endpoint added


### DELETE /assessments_reports/{assessments_report_id}
-  endpoint added


### GET /assessments_reports/{assessments_report_id}
-  endpoint added


### PUT /assessments_reports/{assessments_report_id}
-  endpoint added


### GET /frameworks
-  added the optional property `frameworks/items/maturity_scale_id` to the response with the `200` status


### POST /frameworks
-  added the new optional request property `framework/maturity_scale_id`
-  added the optional property `frameworks/items/maturity_scale_id` to the response with the `200` status


### DELETE /frameworks/{framework_id}
-  added the optional property `maturity_scale_id` to the response with the `200` status


### GET /frameworks/{framework_id}
-  added the optional property `maturity_scale_id` to the response with the `200` status


### PUT /frameworks/{framework_id}
-  added the new optional request property `allOf[#/components/schemas/frameworks_put]/framework/maturity_scale_id`
-  added the new optional request property `allOf[#/components/schemas/frameworks_put]/previous_values/maturity_scale_id`
-  added the optional property `maturity_scale_id` to the response with the `200` status


### GET /global_audits/{global_audit_id}
-  added the enum value `assessmentsReport` to the property `items/` of the `query` request parameter `include`
-  added the enum value `projectPlanContainerCoverageFilter` to the property `items/` of the `query` request parameter `include`






