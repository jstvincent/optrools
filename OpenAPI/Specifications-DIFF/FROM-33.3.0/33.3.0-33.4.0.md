# API Changelog 33.3.0 vs. 33.4.0


## API Changes

### GET /comments
-  added the enum value `projectPlanContainer` to the property `items/` of the `query` request parameter `include`


### GET /comments/{comment_id}
-  added the enum value `projectPlanContainer` to the property `items/` of the `query` request parameter `include`


### GET /files/{file_id}
-  added the enum value `insight` to the property `items/` of the `query` request parameter `include`
-  added the enum value `insights` to the property `items/` of the `query` request parameter `include`
-  added the enum value `projectPlanContainer` to the property `items/` of the `query` request parameter `include`


### GET /global_audits/{global_audit_id}
-  added the enum value `reactiveExpression` to the property `items/` of the `query` request parameter `include`
-  added the enum value `reactiveExpressionVariable` to the property `items/` of the `query` request parameter `include`
-  added the enum value `reactiveExpressionVariableParameter` to the property `items/` of the `query` request parameter `include`


### GET /issue_categories
-  added the optional property `issue_categories/items/intake_smart_form_template_id` to the response with the `200` status


### POST /issue_categories
-  added the new optional request property `issue_category/intake_smart_form_template_id`
-  added the optional property `issue_categories/items/intake_smart_form_template_id` to the response with the `200` status


### DELETE /issue_categories/{issue_category_id}
-  added the optional property `intake_smart_form_template_id` to the response with the `200` status


### GET /issue_categories/{issue_category_id}
-  added the optional property `intake_smart_form_template_id` to the response with the `200` status


### PUT /issue_categories/{issue_category_id}
-  added the new optional request property `allOf[#/components/schemas/issue_categories_put]/issue_category/intake_smart_form_template_id`
-  added the new optional request property `allOf[#/components/schemas/issue_categories_put]/previous_values/intake_smart_form_template_id`
-  added the optional property `intake_smart_form_template_id` to the response with the `200` status


### GET /ops_audit_items
-  added the optional property `ops_audit_items/items/name` to the response with the `200` status


### POST /ops_audit_items
-  added the new optional request property `ops_audit _item/name`
-  added the optional property `ops_audit_items/items/name` to the response with the `200` status


### DELETE /ops_audit_items/{ops_audit_item_id}
-  added the optional property `name` to the response with the `200` status


### GET /ops_audit_items/{ops_audit_item_id}
-  added the optional property `name` to the response with the `200` status


### PUT /ops_audit_items/{ops_audit_item_id}
-  added the new optional request property `allOf[#/components/schemas/ops_audit_items_put]/ops_audit_item/name`
-  added the new optional request property `allOf[#/components/schemas/ops_audit_items_put]/previous_values/name`
-  added the optional property `name` to the response with the `200` status






